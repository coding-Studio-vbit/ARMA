import React from 'react'

function Profile({url,}) {
  return (
    <div>
        {/* {
            url.length>0?
            <img src={url}/>:
            
        } */}
    </div>
  )
}

export default Profile;